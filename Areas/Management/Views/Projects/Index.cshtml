@model IEnumerable<Portfolio.Areas.Management.Models.ProjectViewModel>;
    @{
    ViewData["Title"] = "Projects";
    }
    <div class="absolute w-full mb-12 mt-4 px-4">
        <div class="flex flex-col min-w-0 break-words w-full mb-6 shadow-lg rounded bg-white">
            <div class="rounded-t mb-0 px-4 py-3 border-0">
                <div class="flex flex-wrap items-center">
                    <div class="w-full px-4 max-w-full flex-grow flex-1">
                        <h3 class="font-semibold text-lg">
                            @{
                                CreateProjectViewModel project = new CreateProjectViewModel();
                                <partial name="_CreatePartial" model=@project />
                            }
                        </h3>
                    </div>
                </div>
            </div>

            <div id="recentWorks-table-body" class="block w-full overflow-x-auto">
                <partial name="_TablePartial" model=@Model />
            </div>
        </div>
    </div>